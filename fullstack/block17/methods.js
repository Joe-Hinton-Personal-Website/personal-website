/* array methods
-foreach
  -loop over array from front -> back

          const customeArray = [10, 25, 13]

        customeArray.forEach((currentElement, i) =>) {
          console.log(currentElement, i);

-reduce
  -perform an action on array and return a single value

        const sum = customArray.reduce((sum, currentElement) => currenetElement += sum, 0);
      console.log("sum: ", sum);
-map
  -return a new array with each element doubled
-sort
  -manipulates order of original array

        customArray.sort((a, b) => a - b); ascending

-filter
  -makes a new copy of an array with filtered items

*/


