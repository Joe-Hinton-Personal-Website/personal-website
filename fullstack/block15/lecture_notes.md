// let shoeObject = {
//   brand: "Nike", 
//   laces: true, 
//   velcro: false, 
//   color: ["green", "white", "red", "black"],
//   price: 100,
//   currency: "USD",
//   activityType: "walking"
// };



// // This is how to change a key:value pair
// shoeObject["velcro"] = true;
// console.log(shoeObject);


// // How to add a new key:value pair that does not already exist
// shoeObject["insole"] = false;
// console.log(shoeObject);

// How to delete a key:value pair
// delete shoeObject["activityType"];
// console.log(shoeObject);

// // [Bracket Notation]
// // dot notation (console.log)

// // change a key:value pair that already exist with dot notation
// shoeObject.velcro = true;

// // add new key:value pair
// shoeObject.insole = false;

// // delete existing key:value pair
// delete shoeObject.activityType


// //  how to loop over an entire object
// for (let key in shoeObject) {
//   let value = shoeObject[key];
//   console.log(key, value);
// }

// // get all keys in object
// console.log(Object.keys(shoeObject));

// // get all values in object 
// console.log(Values(shoeObject));

// // get all key:value pairs
// console.log(Object.entries(shoeObject));

// // objects don't have a length

// access a value in an object
// console.log(shoeObject["color"]);

// access an object 
// console.log(shoeObject["random_object"].key2)
